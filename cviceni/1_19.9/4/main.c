#include <stdio.h>

int main()
{
    int a, b, c, max;
    printf("Zadej tri cisla: ");
    scanf("%d %d %d", &a, &b, &c);

    for (i = 0; i < 4; i++)
    {
        if (/* condition */)
        {
            /* code */
        }
    }

    return 0;
}
